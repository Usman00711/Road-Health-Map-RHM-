import { useState } from 'react';
import AppShell from '../components/AppShell';

export default function ProfilePage() {
  const [saved, setSaved] = useState(false);
  return <AppShell><div className="page"><div className="page-heading"><div><p className="eyebrow">Account</p><h1>Administrator profile</h1><p>Manage the identity displayed across the control centre.</p></div></div><section className="simple-grid"><form className="panel form-panel" onSubmit={(event) => { event.preventDefault(); setSaved(true); }}><h2>Profile information</h2><div className="field"><label>Full name</label><div className="input-shell"><input defaultValue="RHM Administrator" /></div></div><div className="field"><label>Email address</label><div className="input-shell"><input defaultValue={localStorage.getItem('rhm_admin') || 'admin@rhm.local'} type="email" /></div></div><div className="field"><label>Role description</label><div className="input-shell" style={{ height: 'auto', padding: 0 }}><textarea defaultValue="Road monitoring and maintenance operations" style={{ width: '100%', border: 0, outline: 0, background: 'transparent' }} /></div></div>{saved && <div className="insight" style={{ marginBottom: 12 }}>Profile preferences saved for this session.</div>}<button className="primary-button">Save changes</button></form><article className="panel form-panel"><div className="avatar" style={{ width: 74, height: 74, borderRadius: 20, fontSize: 25 }}>RA</div><h2 style={{ marginTop: 18 }}>RHM Administrator</h2><p className="login-copy">Authority portal operator with access to road intelligence, feedback, and maintenance tools.</p></article></section></div></AppShell>;
}
