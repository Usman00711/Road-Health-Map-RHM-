import { Bar, BarChart, CartesianGrid, Cell, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import AppShell from '../components/AppShell';
import useOverview from '../useOverview';

export default function Analytics() {
  const { data, error } = useOverview();
  return <AppShell><div className="page"><div className="page-heading"><div><p className="eyebrow">Classification</p><h1>Road analytics</h1><p>Explore how the complete SVM-classified sensor dataset is distributed.</p></div></div>{!data ? <div className={error ? 'error-state' : 'loading-state'}>{error || 'Loading analytics…'}</div> : <section className="simple-grid"><article className="panel"><div className="panel-heading"><div><h2>Samples by condition</h2><p>All 10,023 rows from TotalData.csv</p></div></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.distribution} margin={{ top: 12, right: 20, left: -12, bottom: 0 }}><CartesianGrid stroke="#edf0ed" vertical={false} /><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{ fontSize: 11 }} /><YAxis axisLine={false} tickLine={false} tick={{ fontSize: 10 }} /><Tooltip cursor={{ fill: '#f4f6f2' }} contentStyle={{ borderRadius: 10, border: '1px solid #e3e9e3' }} /><Bar dataKey="value" radius={[8,8,0,0]}>{data.distribution.map((item, index) => <Cell key={item.name} fill={['#1f7a56','#d89a35','#d95b4e'][index]} />)}</Bar></BarChart></ResponsiveContainer></div></article><article className="panel form-panel"><h2>Model context</h2><p className="login-copy">The RBF Support Vector Machine classifies the complete MPU6050 dataset into good, average, and poor road conditions.</p><div className="insight"><strong>Mapped evidence:</strong> the E-11/2 corridor combines six field GPS observations with five inferred good-road segments on an interactive OpenStreetMap route.</div></article></section>}</div></AppShell>;
}
