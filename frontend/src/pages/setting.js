import { useState } from 'react';
import AppShell from '../components/AppShell';

export default function SettingsPage() {
  const [notifications, setNotifications] = useState(true);
  const [saved, setSaved] = useState(false);
  return <AppShell><div className="page"><div className="page-heading"><div><p className="eyebrow">Preferences</p><h1>System settings</h1><p>Configure local dashboard behavior and alerts.</p></div></div><section className="simple-grid"><form className="panel form-panel" onSubmit={(event) => { event.preventDefault(); setSaved(true); }}><h2>Notifications</h2><div className="task-row"><div><strong style={{ fontSize: 12 }}>Poor road alerts</strong><div className="topbar-subtitle">Notify when a sample is classified as poor</div></div><input type="checkbox" checked={notifications} onChange={(event) => setNotifications(event.target.checked)} style={{ marginLeft: 'auto' }} /></div><div className="field" style={{ marginTop: 18 }}><label>Refresh interval</label><div className="input-shell"><select defaultValue="60" style={{ width: '100%', border: 0, outline: 0, background: 'transparent' }}><option value="30">Every 30 seconds</option><option value="60">Every minute</option><option value="300">Every 5 minutes</option></select></div></div>{saved && <div className="insight" style={{ marginBottom: 12 }}>Settings saved locally.</div>}<button className="primary-button">Save preferences</button></form><article className="panel form-panel"><h2>Database</h2><p className="login-copy">This restored setup uses MongoDB 7 in Docker with a persistent local volume. Starting the backend automatically creates the administrator and imports all 10,023 classified sensor samples when the database is empty.</p><div className="insight"><strong>Database:</strong> road-health-map<br /><strong>API:</strong> http://localhost:5050</div></article></section></div></AppShell>;
}
